name: Build
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Prepare
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          show-progress: false
      - uses: gittools/actions/gitversion/setup@v0.10.2
        with:
          versionSpec: '5.12.x'
      - uses: gittools/actions/gitversion/execute@v0.10.2
        id: gitversion

      # Build
      - name: Inject version number
        run: sed -i "s/^version = '.*'$/version = '${{steps.gitversion.outputs.legacySemVer}}'/" 0repo.py
      - uses: 0install/github-actions/0template@v1
        id: feed
        with:
          template: 0repo.xml.template
          version: ${{steps.gitversion.outputs.legacySemVer}}
      # Runs the generated feed locally, so its archive href must stay relative
      - name: 0test
        run: 0install run https://apps.0install.net/0install/0test.xml ${{steps.feed.outputs.feed}}

      # Release
      - name: Get tag message
        if: github.ref_type == 'tag'
        id: tag_message
        run: |
          {
            echo "message<<EOF"
            git fetch -f origin tag ${{github.ref_name}}
            git tag -l ${{github.ref_name}} --format="%(contents)"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
      - name: Create GitHub Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          body: |
            **[Documentation and download instructions](https://docs.0install.net/tools/repo/)**
            ## Changes
            ${{steps.tag_message.outputs.message}}
          files: |
            ${{steps.feed.outputs.feed}}
            ${{steps.feed.outputs.archive}}

      # Publish
      - uses: 0install/github-actions/0repo-submit@v1
        if: github.ref_type == 'tag'
        with:
          repository: 0install/apps
          feed-url: ${{steps.feed.outputs.feed}}
          archive-url: ${{steps.feed.outputs.archive}}
          token: ${{secrets.PERSONAL_TOKEN}}
